# ============================================================
# YouTube Automation System — Requirements
# REQUIRES Python 3.11 or 3.12 (NOT 3.13 or 3.14)
# Windows 11 | CPU only (no GPU) | Last updated: May 2026
#
# Setup: python setup_project.py
# This installs in a venv at .venv/ using Python 3.11
# ============================================================

# ── AI Brain (switchable — set LLM_PROVIDER in .env) ─────────
groq>=0.11.0               # free default — Llama 3.3 70B
anthropic>=0.50.0          # paid optional — Claude Sonnet

# ── Orchestration (LangGraph state machine) ───────────────────
langgraph==1.2.0
langgraph-checkpoint-sqlite==3.1.0

# ── Scheduling ────────────────────────────────────────────────
apscheduler==3.10.4
pytz==2024.1

# ── Notifications (ntfy — no bot setup needed) ────────────────
# ntfy uses plain HTTP requests — no extra package needed
# requests already installed above

# ── Research ──────────────────────────────────────────────────
# praw removed — Reddit API requires manual approval since Nov 2025
# Using free RSS feeds instead (no key needed)
pytrends==4.9.2
requests==2.31.0
newsapi-python==0.2.7
httpx==0.27.0

# ── YouTube / Google APIs ─────────────────────────────────────
google-api-python-client==2.131.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# ── TTS — Kokoro ONNX (CPU, Apache 2.0, Python 3.10-3.13) ────
kokoro-onnx==0.4.7        # requires Python <3.14
onnxruntime==1.24.1       # latest that works on Python 3.11/3.12
soundfile==0.12.1
pydub==0.25.1

# ── Avatar — SadTalker deps (CPU, MIT) ───────────────────────
# SadTalker cloned by setup_project.py
# CPU-only torch — no CUDA, smaller download
# torch installed separately in setup_project.py (whl URL)
face-alignment==1.4.1
imageio==2.34.1
imageio-ffmpeg==0.5.1
resampy==0.4.3
scikit-image==0.23.2
scipy==1.13.0
kornia==0.7.2
facexlib==0.3.0
basicsr==1.4.2

# ── Avatar base image — FLUX.1 schnell (CPU, Apache 2.0) ─────
diffusers==0.27.2
transformers==4.40.2
accelerate==0.30.0
safetensors==0.4.3
sentencepiece==0.2.0

# ── Video Assembly ────────────────────────────────────────────
moviepy==2.2.1
ffmpeg-python==0.2.0
opencv-python==4.9.0.80

# ── Captions (Whisper local, MIT license) ────────────────────
openai-whisper                  # no pin — version depends on setuptools

# ── Thumbnails ────────────────────────────────────────────────
Pillow==10.3.0
colorthief==0.2.1

# ── Charts / Data Viz ─────────────────────────────────────────
matplotlib==3.8.4
plotly==5.22.0
kaleido==0.2.1

# ── Financial Data ────────────────────────────────────────────
alpha-vantage==3.0.0

# ── Analytics / Data ──────────────────────────────────────────
pandas==2.2.2
numpy>=1.26.4

# ── Database ──────────────────────────────────────────────────
supabase==2.30.0           # cloud PostgreSQL — needs new sb_secret_ key format
websockets>=13.0           # required by supabase realtime
# sqlite3 built into Python stdlib — used as local fallback

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.0
colorlog==6.8.2
aiofiles==23.2.1

# ── Future upgrade (when GPU available) ───────────────────────
# Swap SadTalker → MuseTalk: 1 line change in avatar_agent.py
# torch+cuda installed separately
